using AutoMapper;
using Szkolenie.Techniczne3.Infrastucture.Dto;
using SzkolenieTechniczne3.Core.Domain;

namespace Szkolenie.Techniczne3.Infrastucture.Mappers
{
/*    public static class AutoMapperConfig
    {
        public static IMapper Initialize()
        => new MapperConfiguration(cfg =>
        {
            cfg.CreateMap<UserDto, User>();
            cfg.CreateMap<User, UserDto>();
            cfg.CreateMap<Product, ProductDto>().ForMember(x => x.CategoryId, m => m.MapFrom(p => p.CategoryId.Id));
        }).CreteMapper();        
    }*/
}